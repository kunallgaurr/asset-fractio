const PROVIDERS = {
    REDIS_CONNECTION: 'REDIS'
}

const REDIS_KEYS = {
    USERNAME_SET: 'af:username:set',
    EMAIL_SET: 'af:email:set',
}
export const constants = {
    SYNC_DB: true,
    PROVIDERS,
    REDIS_KEYS   
}